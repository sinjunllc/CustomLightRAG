name: Sync Upstream to Main

on:
  schedule:
    - cron: '10 1 * * *' # Runs every day at 1:10 AM UTC
  workflow_dispatch: # Allows for manual triggering

jobs:
  sync-upstream:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Git
        run: |
          git config --global user.name "sinjunllc-bot"
          git config --global user.email "your-email@example.com"

      - name: Add upstream
        run: |
          # Add the upstream repository
          git remote add upstream https://github.com/HKUDS/LightRAG.git
          git fetch upstream
          git checkout main

      - name: Sync with upstream
        run: |
          # Ensure the main branch is clean
          git reset --hard origin/main

          # Fetch the latest changes from upstream
          git fetch upstream main

          # Fast-forward merge
          git merge --ff-only upstream/main

      - name: Debug - Verify Token Access
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "Token Length: ${#GH_TOKEN}"
          if [ -z "$GH_TOKEN" ]; then
            echo "ERROR: GH_TOKEN is empty or not accessible."
            exit 1
          fi

      - name: Push to origin
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          # Explicitly set the token for authentication
          echo "Setting remote URL with token authentication..."
          git remote set-url origin https://x-access-token:${GH_TOKEN}@github.com/sinjunllc/CustomLightRAG.git

          # Verify remote URLs
          echo "Verifying remotes..."
          git remote -v

          # Push the changes to the forked repo
          echo "Pushing changes to origin/main..."
          GIT_ASKPASS=echo git push origin main || exit 1
